algoritmo "multiplicar_por_2"

var
   contador: inteiro

inicio
   contador <- 1

   enquanto (contador <= 10) faca
      escreval(contador, " x 2 = ", contador * 2)
      contador <- contador + 1
   fimenquanto

fimalgoritmo
