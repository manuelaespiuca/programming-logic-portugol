algoritmo "contar_ate_numero"

var
   contador, numero: inteiro

inicio
   escreval("Digite até qual número você quer contar: ")
   leia(numero)

   contador <- 1

   enquanto (contador <= numero) faca
      escreval(contador)
      contador <- contador + 1
   fimenquanto

fimalgoritmo
